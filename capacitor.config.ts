import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.streamocations.app",
  appName: "Stream Ocations",
  webDir: "dist",
};

export default config;
